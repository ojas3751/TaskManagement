# このファイルをコピーして .env を作り、値を埋める。
#   Copy-Item .env.example .env
#
# .env は .gitignore で除外されており、コミットされない。
# docker compose は同じディレクトリの .env を自動で読む。
#
# 注意: バックエンド（Spring Boot）は .env を読まない。
# 同じパスワードをユーザー環境変数 DB_PASSWORD にも設定すること。
# 手順は docs/operations.md「初回セットアップ」を参照。

DB_PASSWORD=
