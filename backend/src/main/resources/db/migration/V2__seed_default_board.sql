-- 初期データ: ボード1件と、デフォルト3列（TODO／進行中／完了）
--
-- 任意の初期データではなく、仕様上必ず存在する前提のもの（F-01）。
-- 空の DB に対して bootRun するだけで使える状態にするため、マイグレーションに含める。
--
-- id は固定値。seed が2回流れても、Flyway が適用済みとして飛ばすため重複しない。

INSERT INTO boards (id, title) VALUES
    ('00000000-0000-4000-8000-000000000001', 'マイタスク');

-- position は 0 始まりの連番（docs/design/database.md「position の採番方式」）。
-- 完了列だけ is_fixed_last = true とし、常に最右に固定する。
INSERT INTO lists (id, board_id, title, is_default, is_fixed_last, position) VALUES
    ('00000000-0000-4000-8000-000000000101',
     '00000000-0000-4000-8000-000000000001', 'TODO',   true, false, 0),
    ('00000000-0000-4000-8000-000000000102',
     '00000000-0000-4000-8000-000000000001', '進行中', true, false, 1),
    ('00000000-0000-4000-8000-000000000103',
     '00000000-0000-4000-8000-000000000001', '完了',   true, true,  2);
