# ブラッシュアップ案 v1.5

**関連文書**: [要件定義書](requirements.md) ／ [機能仕様書](functional-spec.md) ／ [開発計画](development-plan.md) ／ [文書一覧](README.md)

---

## 改訂履歴

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| **1.5** | **2026-09-01** | **C-4 に、キーボード操作でも同じことが起きた実例を足した**（#93 / #97）。**列の並び替えで3件、タスクの移動で5件**の不具合が、すべて触ってみて初めて見つかっている。**原因はどれも「矩形の形と位置」**で、**jsdom には配置が無いため、いまのテスト環境では原理的に再現できない。** 落ち先のロジックの単体テストは通っていた。**方針は変えない**（自動テストは勧めない）が、**手動確認が担っている範囲がはっきりした** |
| **1.4** | **2026-08-31** | **C-1 を確認し、対応した**（#95）。**懸念は当たっていたが、停留点は3つではなく4つだった** — チェックボックス（F-15 / F-22）を数えていなかった。**2つは読み上げ名まで同じ**でもあった。タイトルのボタンを無くしてカード全体で受ける形にし、**3つに減らした。** **ドラッグハンドルを置く案は採らなかった**（使い勝手を失ううえ停留点も減らない）。**3章に残るのは C-3 と C-4 だけ** |
| **1.3** | **2026-08-31** | **②を完了にした**（#93）。**1章はこれで空になった** — 対応すると決めたものは残っていない。あわせて **C-2 を対応済みにした**（構成は変えず、コメントで片付けた）。**C-4（ドラッグの自動テストが無い）は残す** — F-21 の不具合3件も、すべて手動確認でしか見つからなかった。**この文書の次の役割は、2章の保留（F-17〜F-20）と3章の残り（C-1・C-3・C-4）の置き場** |
| **1.2** | **2026-08-31** | **②（F-21 ＋ F-24）に着手した**（#89）。**範囲を「列の並び替え」から「リストに関する操作すべて」へ広げた** — F-02（追加）・F-03（改名）・F-04（削除）も編集モードに入れる。**ねらいは平時のタスク管理を邪魔しないこと。** **決めた内容そのものは [要件定義書](requirements.md) 5.2・[機能仕様書](functional-spec.md) 1.6・[画面設計](design/ui.md) 1.2 に置き、ここには残していない**（この文書は決定の正本ではないため）。**実装の順序は [開発計画](development-plan.md) の Step 12。** あわせて **C-1 と C-2 の扱いを更新した** |
| **1.1** | **2026-08-31** | 1章の①（F-22）を**完了**にした（#87）。**決めた内容そのものは [要件定義書](requirements.md) 5.2 と [機能仕様書](functional-spec.md) 3.4 に置き、ここには残していない。** 4章の #20 も完了済み（#86）。**残るは②（F-21 ＋ F-24）だけ** |
| 1.0 | 2026-08-30 | **MVP 完成にあわせて新設。** それまで [要件定義書](requirements.md) 5.2・コード上のコメント・会話に散っていた「この先やること」を1か所に集めた。あわせて2つを決めた。①**着手する順**（F-22 → F-21＋F-24）②**F-21 と F-24 を1つの機能として作る**（#84） |

### この文書の役割

**MVP 完成後にやることの候補を置く。** 探す場所を1つにするための文書であり、**決定の正本ではない。**

| 種類 | 正本 |
| --- | --- |
| **何を作るか**（F-xx の定義・MVP に入れない理由） | [要件定義書](requirements.md) 5.2 |
| **各機能の仕様と受け入れ条件** | [機能仕様書](functional-spec.md) |
| **実装の順序** | [開発計画](development-plan.md) |

ここに書くのは、**上の文書には収まらないもの**（着手の優先順、複数機能をまたぐ方針、実装面の改善案）に限る。同じ内容を両方に書くと片方だけが古くなるため、**重なる部分はリンクで指す。**

**着手するときは Issue を立てる。** この文書は候補の一覧であって、作業の管理はしない。

---

## 1. 対応すると決めたもの

**この順で着手する。**（2026-08-30 決定、#84）

| 順 | ID | 内容 | 状態 |
| --- | --- | --- | --- |
| **①** | F-22 | **ホバーでの完了操作。** 「完了」以外の列のタスクにマウスを乗せるとチェックボックスが現れ、チェックすると「完了」列の**先頭**へ移動する | **完了**（2026-08-31、#87） |
| **②** | **F-21 ＋ F-24** | **リストに関する操作を「編集モード」に集約する**（下記） | **完了**（2026-08-31、#89 / #91 / #93） |

> **①②とも、決めたことは [要件定義書](requirements.md) 5.2 と [機能仕様書](functional-spec.md) に移した。** ここには残さない（この文書は決定の正本ではないため）。

### ② リストの操作を編集モードに集約する

**当初は「列の並び替え（F-05 ＋ F-21）だけをモードに入れる」計画だったが、2026-08-31 に範囲を広げた**（#89）。**F-02（追加）・F-03（改名）・F-04（削除）も入れる。**

| 探しているもの | 正本 |
| --- | --- |
| **何をどこまでモードに入れるか・なぜか** | [要件定義書](requirements.md) 5.2 の F-24 |
| **モードの内と外で何が出るか・受け入れ条件** | [機能仕様書](functional-spec.md) 1.6 と 5.1 |
| **画面の見た目** | [画面設計](design/ui.md) 1.2 |
| **実装の順序（3つの PR の切り方）** | [開発計画](development-plan.md) の Step 12 |

---

## 2. 保留するもの

**取り下げではない。** 必要になったら着手する。

| ID | 内容 |
| --- | --- |
| F-17 | ラベル（色タグ） |
| F-18 | タスクの検索・絞り込み |
| F-19 | ボードの複数管理 |
| F-20 | データのバックアップ（書き出し・読み込み） |

定義と、MVP に入れなかった理由は [要件定義書](requirements.md) 5.2 にある。

---

## 3. 実装面の改善案

**機能の追加ではなく、いまあるものの手直し。** F-xx の番号は振らない。

### ~~C-1. カード全体に `role="button"` と `tabIndex` が付いている可能性~~ **確認・対応済み**（2026-08-31、#95）

**確認した。懸念は当たっていたが、数は1つ多かった。** カード本体（`<article>`）に実際に付いていたもの。

```
role = button
tabindex = 0
aria-disabled = false
aria-roledescription = sortable
aria-describedby = DndDescribedBy-0
```

**Tab の停留点は3つではなく4つだった。**

| # | 要素 | 読み上げ名 |
| --- | --- | --- |
| 1 | チェックボックス | 「牛乳を買う」を完了にする |
| 2 | カード本体 | 牛乳を買う（ボタン、ソート可能） |
| 3 | タイトルのボタン | 牛乳を買う |
| 4 | ゴミ箱 | 「牛乳を買う」を削除 |

**この文書が「3つ」と書いていたのは、F-15 と F-22 でチェックボックスがどの列のタスクにも出るようになった分を数えていなかったため。** 20件の列なら 80 回。**2と3は読み上げ名まで同じ**で、同じ言葉を2回聞くことになっていた。

**対応は、タイトルのボタンを無くしてカード全体で受ける形。** 停留点は **3つ**になり、名前の重複も消えた。**キーは `Enter`（開く）と `Space`（掴む）で分けた。** 仕様の正本は [機能仕様書](functional-spec.md) 2.8 と [画面設計](design/ui.md) 2章。

> **専用のつまみ（ドラッグハンドル）を置く案は採らなかった。** この文書が警戒していたとおり**カード全体を掴める使い勝手を失う**うえ、**停留点も減らない。** `role="button"` だけを外す案も、`<article>` の意味は戻るが**停留点が減らない**ため採らなかった。

> **列（F-21）では同じ問題にならない。** 列も列全体を掴む形にしたので `<section>` に同じ属性が付くが、**増える停留点は列数ぶん（3〜10）で、中のタスクのように件数分は増えない。** 編集モード中はタスク側が inert なので、**モード中の停留点はむしろ減っている。**

### ~~C-2. `SortableContext` の並び替え戦略が使われていない~~ **対応済み**（2026-08-31、#93）

落ち先を線で示す方式にしたとき、`useSortable` が返す `transform` を当てるのをやめた（[画面設計](design/ui.md) 3章）。その結果 `verticalListSortingStrategy`（カードをずらす計算）は**計算されるが誰も使わない**状態になっている。

いま `SortableContext` が実際に担っているのは、**キーボード操作の移動先計算**と id の管理だけ。

**動作に害は無い。** ただし [`ListColumn`](../frontend/src/components/ListColumn.tsx) を読んだ人が「ここでカードが動く」と誤解する。**コメントを足すか、構成を整理するか**のどちらか。

> **コメントで片付けた。**（2026-08-31、#93）**構成は変えていない。** F-21 で列にも `SortableContext` を足したので、[`ListColumn`](../frontend/src/components/ListColumn.tsx) と [`BoardView`](../frontend/src/components/BoardView.tsx) の両方に「**実際に担っているのはキーボード操作の移動先計算と id の管理だけで、ずらす計算は使われない**」と書いた。**2つは役割が同じで向きが違うだけ**であることも、互いを指して書いてある。

### C-3. 応答待ちの間は盤面全体が操作できない

[`App`](../frontend/src/App.tsx) は「**飛んでいるリクエストは常に1本**」を前提に、失敗したら盤面ごと巻き戻す設計になっている。**ロックを外すとこの前提が崩れる**（控えているのが盤面全体なので、待っている間の操作まで巻き戻す。#43 で実際に不具合が出た）。

**いまは体感の問題になっていない。** 正常時の応答は 10ms 前後で、待機表示すら出ないため。

**着手するとしたら、困ってから。** タスクが増えるか、通信の遅い環境で使うようになったときに効いてくる。**設計の変更**であり、部分的な手直しでは済まない。

### C-4. ドラッグ&ドロップの自動テストが無い

落ち先を求めるロジック（[`dropTarget.ts`](../frontend/src/lib/dropTarget.ts) / [`moveCard.ts`](../frontend/src/lib/moveCard.ts)）にはテストがあるが、**掴んで落とす操作そのもの**は手動確認だけ。

**Step 11 で見つかった不具合は、すべて目視で見つかった。** 列の外に出られない／元の位置に戻る／2つ先と3つ先の間だけ挙動が違う／線が1px はみ出してスクロールバーが出る、のいずれも。

**積極的には勧めない。** ドラッグの自動テストは座標を伴うため費用対効果が悪い。**ただし「手動確認頼みである」ことは意識しておく。**

> **その後、同じことがキーボード操作でも起きた。**（2026-09-01、#93 / #97）**列の並び替えで3件、タスクの移動で5件**の不具合が、すべて**触ってみて初めて**見つかっている。掴んだものが下へ沈む／引き返すと1回目が空振りする／別の列で元の列の行数ぶん下がる／一番上に置けない／元いた列へ戻れない、など。
>
> **原因はどれも「矩形の形と位置」**で、**jsdom には配置が無い**（すべて 0 になる）ため、いまのテスト環境では**原理的に再現できない。** 落ち先を求めるロジックの単体テストは通っていた。
>
> **見つけたのは、症状を正確に言葉にした報告。** 「1回目だけ空振りする」「2件の列なのに4件の列の3番目に入る」といった具体的な言い方が、そのまま原因の切り分けになっていた。

---

## 4. 既に Issue になっているもの

| Issue | 内容 |
| --- | --- |
| [#20](https://github.com/ojas3751/TaskManagement/issues/20) | `[+ タスク追加]` を「進行中」「完了」で非表示にする |

**#20 は着手済み**（2026-08-31）。**このとき2つを決めた。**

| 決めたこと | 内容 |
| --- | --- |
| **消すのは「完了」列だけ** | Issue 本文の「進行中でも非表示」は取りやめ。**完了列以外は「そこへ直接起票したいか」の判断が、この文書 1章のブラッシュアップ案（特に F-22 のホバーでの完了操作）の前提と混ざり、いま決められないため** |
| **空いた1行は選択の行で埋める** | `[全選択] [選択解除] [〇件を削除]` を**完了列に常時表示**し、0件のときは押せない状態にする。入れ替えをやめたのは、そのままだと**完了列だけタスクの開始位置が1行ぶん上がる**ため（[画面設計](design/ui.md) 1章） |

仕様の正本は [画面設計](design/ui.md) v1.10 と [機能仕様書](functional-spec.md) v1.10 に移した。

