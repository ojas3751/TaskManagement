# 画面設計 v1.3

**関連文書**: [要件定義書](../requirements.md) ／ [機能仕様書](../functional-spec.md) ／ [文書一覧](../README.md)

**画面はボード画面1つのみ。** 認証を対象外としたため、ログイン画面は存在しない。

本書の図は**ワイヤーフレーム相当**であり、**要素の有無と配置を示すもの**。寸法・余白・書体・具体的な配色は含まない（[要件定義書](../requirements.md) の「未決定事項」のとおり未定）。

> **本書では「タスク」と書く。** DBのテーブル名とAPIのパスは `cards` のままであり、これは意図的な使い分け（[要件定義書](../requirements.md) の「用語」を参照）。
>
> **タスクに移動ボタンは付けない。** 移動はドラッグ&ドロップ（F-13）と詳細モーダルのリスト選択欄（F-23）で行う。

---

## 改訂履歴

| 版 | 日付 | 変更内容 |
| --- | --- | --- |
| 1.0 | 2026-08-13 | 要件定義書 v0.7 から分割して初版作成。内容の変更なし |
| 1.1 | 2026-08-14 | 画面モックでの確認結果を反映。①名称を「タスク」に統一 ②**「+ タスク追加」を列の最上部へ移動**し、新規タスクがその直下に入るよう図を修正 ③**F-13 実装後の画面（移動ボタンなし）を別図として追加** |
| 1.2 | 2026-08-14 | 要件定義書 v0.9 を反映。**移動ボタンを作らないことが決まり、段階による画面の違いが消えたため、2つあったボード画面の図を1つに統合。** 詳細モーダルに**リストの選択欄**（F-23）を追加。**ドラッグ中の表示**の節を新設 |
| 1.3 | 2026-08-15 | 画面モックでの確認結果を反映。①タスクの図に**ゴミ箱アイコン**を追加し、期限と同じ行に置くことを明示 ②**「名前の入力モーダル」「削除の確認モーダル」の節を新設**（`prompt()` / `confirm()` を使わないため）③エラー時の表示を7章へ繰り下げ |

---

## 1. ボード画面（全体）

```
┌──────────────────────────────────────────────────────────────────────┐
│  マイタスク                                                           │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│ [←] TODO [→] │ [←] 進行中[→]│ [←] 設計 [→] │ 完了                   │
│              │              │   [改名][削除]│                        │
│              │              │              │ [☐ すべて選択]         │
│ [+ タスク追加]│ [+ タスク追加]│ [+ タスク追加]│ [+ タスク追加]         │
│ ┏━━━━━━━━━━┓ │ ┌──────────┐ │              │ ☐┌───────────────────┐│
│ ┃ 請求書送付 ┃ │ │ 資料作成  │ │              │  │ 打合せ    08/01    ││
│ ┃ 08/10 09:00┃ │ │ 08/15    │ │ （タスクなし）│  └───────────────────┘│
│ ┗━━━━━━━━━━┛ │ └──────────┘ │              │ ☐┌───────────────────┐│
│ ┏━━━━━━━━━━┓ │              │              │  │ 見積提出           ││
│ ┃ 買い物      ┃ │              │              │  └───────────────────┘│
│ ┃ 08/16      ┃ │              │              │      ：（9件目まで）   │
│ ┗━━━━━━━━━━┛ │              │              │  ┌───────────────────┐│
│              │              │              │  │ 10件目（上部のみ）  ││
│              │              │              │  ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈ │
│              │              │              │      [∨ もっと見る]    │
│              │              │              │ [選択したものを削除]    │
└──────────────┴──────────────┴──────────────┴────────────────────────┘
                                        [+ リスト追加]
                                     （「完了」の左隣に挿入される）
```

| 要素 | 補足 |
| --- | --- |
| `[←] [→]`（列見出し） | 列の並び替え（F-05）。「完了」には表示しない。**列については残す**（代替手段の F-21 が MVP に無いため） |
| `[改名] [削除]` | **追加した列にのみ表示**。デフォルト3列には出さない（F-03, F-04） |
| **`[+ タスク追加]`** | **列の最上部。** 押して入力すると、**すぐ下（列の先頭）**にタスクが入る（F-06）。「完了」列では `[☐ すべて選択]` の下 |
| **タスクの移動ボタン** | **存在しない。** 移動はドラッグ&ドロップ（F-13）と詳細モーダル（F-23）で行う。F-12 は欠番 |
| `☐` チェックボックス | **「完了」列のタスクにのみ表示**（F-15） |
| `[☐ すべて選択]` | 「完了」列の見出し直下 |
| （タスクなし） | **空状態。** タスクが0件の列に表示する。**ここにもドロップできる** |
| `：` | 図の省略記号（実際の画面には表示しない） |
| 10件目 | 上部だけが覗く高さで切れる（F-14） |
| `[削除]`（タスク） | **図では省略している。出し方は未決定**（常時表示・ホバー時のみ・詳細モーダル内へ移動）。移動ボタンが無くても削除ボタンが常時表示では閲覧性の問題が半分しか解決しないため、実物を見て決める |

> **`[+ タスク追加]` を最上部に置く理由。** 追加ボタンが上端にあるのに追加物が末尾に現れると、追加したものが視界の外に出る。9件を超える列ではスクロールしないと確認できず、登録できたか分からない。**操作した位置と、結果が現れる位置を一致させる。**
>
> **タスクに移動ボタンを置かない理由。** ボタンが件数分だけ縦に積み上がると、1件あたりの高さが増えて**1画面に収まるタスクが減る**。一覧して状況を把握することがボードの主目的である以上、そこを削ってまで操作手段を増やさない（[機能仕様書](../functional-spec.md) の「タスクの移動・並び替え」を参照）。

---

## 2. タスク（拡大）

```
 ┏━━━━━━━━━━━━━━━━━━━━┓  ← 期限による色付きボーダー（F-11）
 ┃ 請求書送付            ┃  ← タイトル（クリックで詳細モーダル）
 ┃ 08/10 09:00      [🗑] ┃  ← 期限とゴミ箱を同じ行に。ゴミ箱は右端
 ┗━━━━━━━━━━━━━━━━━━━━┛     掴んでドラッグすると移動する

 期限がない場合も行の高さは変えない
 ┏━━━━━━━━━━━━━━━━━━━━┓
 ┃ 読みたい本を探す      ┃
 ┃                  [🗑] ┃  ← 期限の位置は空けたまま
 ┗━━━━━━━━━━━━━━━━━━━━┛
```

一覧に表示するのは**タイトルと期限のみ**。説明文は表示しない。

| 要素 | 補足 |
| --- | --- |
| ゴミ箱アイコン | **削除（F-08）。** 「削除」の文字ボタンにはしない。**常時表示**とし、ホバー時のみにはしない |
| 配置 | **期限と同じ行の右端。** 削除のためだけに行を増やさない |
| 色 | **タスクの文字色に追従させる。** 期限超過・当日（白文字）でも完了列（黒文字）でも判別できること |
| 期限なしの場合 | **行の高さは揃える。** 期限の有無で高さが変わると縦の並びがばらつく |

> **アイコンは実装側の図形描画で作る。** 画像ファイルは持たない。絵文字（🗑）も使わない — フォントによって字形も色も変わり、文字色への追従ができないため。**上図の `🗑` は図中での便宜的な表記**であり、実装に絵文字を使う意味ではない。

---

## 3. ドラッグ中の表示（F-13）

**どこに落ちるかを、指を離す前に分かるようにする。** 落ちた場所が予想と違うと、利用者は毎回結果を確認してからやり直すことになる。

```
   移動元                        移動先（「進行中」列）
 ┌──────────────┐            ┌──────────────┐
 │ [+ タスク追加]│            │ [+ タスク追加]│
 │ ┌──────────┐ │            │ ┌──────────┐ │
 │ │ 資料作成  │ │            │ │ 資料作成  │ │
 │ └──────────┘ │            │ └──────────┘ │
 │ ┌╌╌╌╌╌╌╌╌╌╌┐ │            │ ━━━━━━━━━━━━ │ ← 挿入位置を示す線
 │ ╎ 請求書送付╎ │ …掴まれた   │ ┌──────────┐ │
 │ └╌╌╌╌╌╌╌╌╌╌┘ │   元の位置  │ │ 打合せ    │ │
 │              │   （薄く）  │ └──────────┘ │
 └──────────────┘            └──────────────┘

                  ┏━━━━━━━━━━┓
                  ┃ 請求書送付 ┃ ← ポインタに追従する本体
                  ┃ 08/10 09:00┃
                  ┗━━━━━━━━━━┛
```

| 要素 | 仕様 |
| --- | --- |
| 掴まれたタスクの元の位置 | 薄く表示し、まだそこに属していることを示す |
| ポインタに追従する本体 | 掴んでいるタスクの見た目を保つ |
| **挿入位置の線** | ポインタのY座標が、どのタスクの前後に入るかを示す。**列をまたいでも同様に出す** |
| 空状態の列 | 「（タスクなし）」の領域全体が受け口になる |
| 「完了」列 | **折りたたまれた状態のままドロップを受け付ける。** 上位9件が見えているため、差し戻し（UC-03）も完了への移動も成立する |
| ドロップ範囲の外で離した場合 | 移動しない。元の位置に戻る |

---

## 4. タスクの詳細モーダル（F-07, F-23）

タスクのタイトル部分をクリックすると開く。

```
      ┌────────────────────────────────────────────┐
      │ タスクの詳細                          [ ✕ ] │
      ├────────────────────────────────────────────┤
      │ リスト                                      │
      │ ┌──────────────────┐                       │
      │ │ TODO           ▼ │                       │  ← 移動先を選ぶ（F-23）
      │ └──────────────────┘                       │
      │                                            │
      │ タイトル                                    │
      │ ┌────────────────────────────────────────┐ │
      │ │ 請求書送付                              │ │
      │ └────────────────────────────────────────┘ │
      │                                      5/100 │  ← 文字数カウンタ
      │                                            │
      │ 説明文                                      │
      │ ┌────────────────────────────────────────┐ │
      │ │ 8月分の請求書をまとめて送付する。        │ │
      │ │                                        │ │  ← 改行を保持
      │ │ ・A社                                   │ │
      │ │ ・B社                                   │ │
      │ │                                        │ │
      │ └────────────────────────────────────────┘ │
      │                                   348/5000 │
      │                                            │
      │ 期限                                        │
      │ ┌──────────────┐  ┌──────────┐            │
      │ │ 2026/08/10   │  │  09:00   │            │
      │ └──────────────┘  └──────────┘            │
      │ [ 期限をクリア ]   ☑ 時刻を指定する         │
      │                                            │
      ├────────────────────────────────────────────┤
      │                    [ キャンセル ] [ 保存 ] │
      └────────────────────────────────────────────┘
```

| 要素 | 補足 |
| --- | --- |
| **`リスト` の選択欄** | **F-23。** 現在の所属リストを表示し、変更して `[ 保存 ]` すると**そのリストの末尾へ移動する**。選択肢は「完了」を含む全リスト |
| 文字数カウンタ | 入力欄の右下。上限到達で赤 |
| `☑ 時刻を指定する` | **`cards.has_due_time` に対応。** 外すと時分の入力欄が無効になり、表示からも時分が消える |
| `[ 期限をクリア ]` | `due_at` を NULL に戻す（期限なし） |
| 閉じ方 | `[ ✕ ]`、`[ キャンセル ]`、背景クリックのいずれか |

> **リストの選択欄をタイトルより上に置く理由。** そのタスクが「今どこにあるか」は、内容を読む前に把握したい情報だから。また **`[ 保存 ]` までキーボードのタブ移動だけで到達できる位置**にあることで、マウスを使わない移動操作（F-23 の主目的）が自然な順序になる。

---

## 5. 名前の入力モーダル（F-02, F-03, F-06）

**`prompt()` は使わない**（[機能仕様書](../functional-spec.md) の「誤操作・エラーへの対策」を参照）。

```
      ┌────────────────────────────────────┐
      │ タスクの追加                  [ ✕ ] │  ← 場面に応じて見出しが変わる
      ├────────────────────────────────────┤
      │ タイトル                            │
      │ ┌────────────────────────────────┐ │
      │ │ 請求書送付                      │ │
      │ └────────────────────────────────┘ │
      │                             5/100  │  ← 文字数カウンタ（E-01）
      │ 入力してください。                  │  ← 空欄で確定した場合のみ
      ├────────────────────────────────────┤
      │            [ キャンセル ] [ 追加 ] │
      └────────────────────────────────────┘
```

| 場面 | 見出し | 入力欄 | 上限 | 実行ボタン |
| --- | --- | --- | --- | --- |
| タスクの追加（F-06） | タスクの追加 | タイトル | 100 | 追加 |
| リストの追加（F-02） | リストの追加 | リスト名 | 50 | 追加 |
| リスト名の変更（F-03） | リスト名の変更 | リスト名 | 50 | 保存 |

リスト名の変更では、**現在の名前が入った状態**で開く。確定は実行ボタンまたは `Enter`。

---

## 6. 削除の確認モーダル（F-04, F-08, F-15）

```
      ┌────────────────────────────────────┐
      │ 削除の確認                          │
      ├────────────────────────────────────┤
      │ リスト「設計」を削除します。        │
      │ 中のタスク 3 件も一緒に削除されます。│
      │ この操作は取り消せません。          │
      ├────────────────────────────────────┤
      │          [ キャンセル ] [ 削除する ]│
      └────────────────────────────────────┘
              ↑ 初期フォーカス      ↑ 赤系
```

| 操作 | 文言に含めるもの |
| --- | --- |
| タスクの削除（F-08） | タスク名 |
| リストの削除（F-04） | リスト名 ＋ **道連れになるタスクの件数** |
| 完了列の選択削除（F-15） | 選択件数 |

いずれも「この操作は取り消せません」を添える（Undo は対象外のため）。

> **初期フォーカスは「キャンセル」に置く。** 開いた直後に `Enter` を押しても削除されないようにするため。

---

## 7. エラー時の表示

挙動の定義は [機能仕様書](../functional-spec.md) の「誤操作・エラーへの対策」を参照。

### 7.1 初期読み込みに失敗した場合

ボードを表示できないため、全面にエラーを出す。

```
┌───────────────────────────────────────────────────────────────────┐
│  マイタスク                                                        │
├───────────────────────────────────────────────────────────────────┤
│                                                                   │
│                              ⚠                                    │
│                                                                   │
│                 サーバーが起動していません。                        │
│              起動してから再読み込みしてください。                    │
│                                                                   │
│                        [ 再読み込み ]                              │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

### 7.2 操作中に失敗した場合

ボードは表示されたまま、画面上部にメッセージを出す。**あわせて画面の表示を操作前の状態に戻す**（ロールバック）。

```
┌───────────────────────────────────────────────────────────────────┐
│ ⚠ データベースに接続できません。                            [ ✕ ] │
│   Docker が起動しているか確認してください。                        │
├───────────────────────────────────────────────────────────────────┤
│  マイタスク                                                        │
├──────────────┬──────────────┬──────────────┬─────────────────────┤
│ [←] TODO [→] │ [←] 進行中[→]│ [←] 設計 [→] │ 完了                │
│              （ボードはそのまま表示され、操作を続けられる）          │
└──────────────┴──────────────┴──────────────┴─────────────────────┘
```
